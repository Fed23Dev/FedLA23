# resource :config
exp_name: emnist-resnet110

# checkpoint config
model: resnet110
alg: fedavg
gpu_ids: [0, 1]
# pre_train: True

# dataset config
dataset: emnist
batch_size: 64

# optimizer config
# optim: sgd
optim: sgd
learning_rate: 0.01

# scheduler config
scheduler: warmup_cos_lr
warm_steps: 50
min_lr: 0.0

# federal config
federal: True
non_iid: hetero
workers: 62
active_workers: 6
federal_round: 500

local_epoch: 10
test_batch_limit: 5
